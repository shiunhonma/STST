<%= form_with(model: chat) do |form| %>
  <% ifha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralizeha.errors.count, "error") %> prohibited thisha from being saved:</h2>

      <ul>
        <%ha.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "メッセージ" %>
    <%= form.text_area :message %>
  </div>

  <div class="field">
    <%= form.label "添付ファイル" %>
    <%= form.text_field :attachment %>
  </div>

  <div class="field">
    <%= form.label "ユーザID" %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
