<%= form_with(model: user_room) do |form| %>
  <% if user_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_room.errors.count, "error") %> prohibited this user_room from being saved:</h2>

      <ul>
        <% user_room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.number_field :room_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
