<p id="notice"><%= notice %></p>

<p>
  <strong>コメント:</strong>
  <%= @room_entry.comment %>
</p>

<p>
  <strong>備考:</strong>
  <%= @room_entry.flag %>
</p>

<% if current_user.id == @room_entry.room.user_id %>
  <%= link_to '編集', edit_room_entry_path(@room_entry) %> |
<% end %>
<%= link_to '戻る', room_categories_path %>
